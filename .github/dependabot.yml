# Keeps GitHub Actions pins patched. Free on private repos.
# All action bumps grouped into one weekly PR.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      github-actions:
        patterns:
          - "*"
